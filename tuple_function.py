# following are the built-in tuple function

t = (1, 2, 3, 4, 5, 6)
t2 = (1, 2, 3, 4, 5, 6)

print(t.count(1))  # count() functions counts are repentance of an element

print(t.index(4))  # index() function tells that element exists at which index

